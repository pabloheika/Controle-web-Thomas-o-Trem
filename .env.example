# Copie este arquivo para .env.local e altere SOMENTE o IP abaixo.
# Todas as partes do projeto (app + proxy Vite) usam VITE_ESP_TARGET.

VITE_ESP_TARGET=

# Em dev, requisicoes passam por http://localhost:5173/esp/... (evita bloqueio do Chrome)
VITE_ESP_USE_PROXY=true
